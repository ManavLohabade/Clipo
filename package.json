{
  "name": "clipper-dapp",
  "version": "1.0.0",
  "description": "Clipper DApp - Blockchain-powered campaign engagement platform",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "contracts"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "test": "npm run test:contracts",
    "test:contracts": "cd contracts && npm run test",
    "compile:contracts": "cd contracts && npm run compile",
    "deploy:contracts": "cd contracts && npm run deploy:fuji",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../contracts && npm install",
    "clean": "npm run clean:backend && npm run clean:frontend && npm run clean:contracts",
    "clean:backend": "cd backend && rm -rf dist node_modules",
    "clean:frontend": "cd frontend && rm -rf .next node_modules",
    "clean:contracts": "cd contracts && rm -rf artifacts cache node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "keywords": [
    "avalanche",
    "blockchain",
    "campaign",
    "engagement",
    "web3",
    "dapp"
  ],
  "author": "Clipper DApp Team",
  "license": "MIT"
}
